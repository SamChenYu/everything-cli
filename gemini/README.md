# Basic Gemini Wrapper
- `https://ai.google.dev/gemini-api/docs` to get API_KEY then populate .env
- For stupid questions mainly

Setup
```
python3 -m venv .venv
source .vnev/bin/activate
pip3 install -r requirements.txt
```
